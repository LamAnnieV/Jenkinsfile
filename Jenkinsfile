pipeline {
  agent any

  stages {
    stage ('Build') {
      steps {
        sh "echo Run a build command"
     }
   }
    stage ('test') {
      steps {
        sh "echo Run a test command"
      }
    }
        stage ('Deploy') {
      steps {
        sh "echo Run a deploy command"
     }
   }
  }
}
